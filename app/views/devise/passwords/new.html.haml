- title 'パスワードを忘れた - ポータル'

.page-header
  %h2 パスワードを忘れた

.row
  .span10
    = form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f|
      -if resource.errors.any?
        .alert-message.block-message.error
          %h3= "#{resource.errors.count} 件のエラーが発生したため、処理できませんでした。"
          %ul
            - resource.errors.full_messages.each do |msg|
              %li= msg
      
      %fieldset
        .clearfix
          = f.label :email
          .input
            = f.email_field :email
        .actions
          = f.submit "パスワードを再設定するためのメールを送る", :class => 'btn primary'
    
  .span6
    = render :partial => "devise/shared/links"
